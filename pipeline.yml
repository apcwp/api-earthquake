resources:
  - name: api-earthquake-source
    type: git
    source:
      uri: https://github.com/techwizblog/api-earthquake.git
      branch: master

jobs:
- name: api-earthquake-ci
  serial: true	
  plan:
  - {get: api-earthquake-source, trigger: true}
  - task: build
    privileged: true
    file: api-earthquake/build.yml
     