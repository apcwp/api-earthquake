platform: linux

   
image_resource:
  type: docker-image		
  source: 
    repository: maven 

inputs: 
- name: built-project

outputs: 
- name: test-project

run:
  path: sh
  args:
   - -exc
   - |
     ls -la
     mkdir /tmp/build/built-project
     cd /tmp/build/built-project
     echo api-earthquake-release >> demo.txt
     echo V1.0 >> tag.txt	 
     ls -la
     java -version
params:
  TERM: -dumb
  GRADLE_OPTS: -Dorg.gradle.native=false	