platform: linux

   
image_resource:
  type: docker-image		
  source: 
    repository: maven 

inputs: 
- name: built-project

outputs: 
- name: test-project

run:
  path: sh
  args:
   - -exc
   - |
     ls -la
     cd built-project
     cat > demo.txt
     car > tag.txt	 
     ls -la
     java -version
params:
  TERM: -dumb
  GRADLE_OPTS: -Dorg.gradle.native=false	